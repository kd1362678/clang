
#include<stdio.h>
#include<stdlib.h>
#include<time.h>
msin() {

	int i, j, k;
	int d[10];
	for (i = 0; i != 10; i++) {
		srand(time(0));
		d[i] = rand();
	}
	for (i = 0; i != 10; i++) {
		for(j)
	}
}